# Profil "local" (mvn spring-boot:run -Dspring-boot.run.profiles=local).
# A surcharger selon votre poste ; ne contient rien de sensible par defaut.
spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.SQL: DEBUG
